rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Firebase Authentication で認証されたユーザーのみに読み書きを許可する設定
      allow read, write: if request.auth != null;
    }
  }
}
