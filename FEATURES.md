# 日本酒ペアリングラボ (Sake Pairing Lab) 現状機能一覧

本書は、日本酒ペアリングラボのアプリケーションに実装されている現状の機能を体系的に洗い出したドキュメントです。

---

## 1. アプリケーション概要

日本酒ペアリングラボは、日本酒のスペックだけでなく、**「飲用温度」「酒器」「ペアリングする料理」などの文脈（コンテキスト）** と **「香り」「味わい」の体験（リザルト）** を共有・探求できるWebプラットフォームです。  
Gemini API による埋め込み（Embedding）生成と Firestore のベクトル検索、さらに多次元データの3D/ネットワーク可視化技術を組み合わせています。

---

## 2. 機能カテゴリ別 詳細一覧

### 2.1 ユーザー認証・アカウント管理

- **Google認証ログイン**: Firebase Authentication によるワンクリックGoogleサインイン / ログアウト
- **プロフィール表示・管理**:
  - 表示名、アバター画像、貢献スコア（Contribution Score）、称号（Title）の表示
  - 表示名の変更（設定画面）
- **個人用 Gemini API キー設定**:
  - ユーザー個人の Gemini API キーをブラウザ（localStorage）に安全に保存・管理
  - APIキーを登録することで、AIレビュー生成や画像生成などの高度な機能を利用可能

### 2.2 日本酒データ管理・詳細

- **日本酒の新規登録 (`/sake/new`)**:
  - 酒蔵名、銘柄、種別（特定名称酒/ボトル）、特徴・説明文の登録
  - 登録時にバックエンドで自動ベクトル化
- **日本酒詳細表示 (`/sake/:id`)**:
  - 基本スペック（酒蔵、銘柄、ボトル、説明）
  - AI味わいアート（Flavor Image）の表示および生成・再生成
  - 投稿されたレビュー一覧（ページネーション読み込み）
  - **類似日本酒レコメンド**: ベクトル検索（コサイン類似度）により特徴の近いお酒を提案

### 2.3 レビュー・テイスティング記録

- **多面的テイスティングレビュー投稿 (`/sake/:id/review`)**:
  - **飲用温度**: 雪冷え(5℃)〜飛び切り燗(55℃〜)の9段階プリセット
  - **酒器**: ワイングラス、平盃、お猪口、薄張りグラス、陶器、木枡などのプリセット＋カスタム入力
  - **ペアリング（おつまみ・料理）**: 定番料理プリセット＋自由入力
  - **香り・味わいの階層選択**:
    - 香りツリー（フルーティ、フローラル、ハーブ・スパイス、穀物・乳製品、熟成など）
    - 味わいツリー（スッキリ・軽快、ふくよか・旨味、甘味など）
  - **相性評価**: 合わない、合う、ピッタリ（段階評価）
  - **コメント入力**: 自由記述
- **AIコメント生成アシスト (Gemini 3.6 Flash)**:
  - 選択した特徴ワードと、ユーザーの過去投稿レビュー履歴（最新最大30件の文体や好み）を分析し、自然なテイスティングコメント（50文字程度）をワンタップ自動生成
- **タイムライン & レビュー表示 (`/`, `/mypage`)**:
  - ホーム画面での新着レビュータイムライン（無限スクロール / ページネーション）
  - レビューへの「いいね（Likes）」カウント・リアクション機能

### 2.4 クエスト・ゲーミフィケーション

- **クエスト一覧・挑戦 (`/quests`)**:
  - ユーザーが作成した「お題（クエスト）」の一覧表示
  - 指定されたお酒（または自由なお酒）に対して、特定の温度・酒器・ペアリングの組み合わせを検証
  - クエスト達成で「貢献スコア（Reward Points）」を獲得
- **新規クエスト作成 (`/quests/new`)**:
  - 特定の日本酒を指定、または日本酒自由のクエスト作成
  - 目標の温度帯、酒器、ペアリング料理の指定
  - タイトル・説明・報酬ポイントの設定

### 2.5 ベクトル検索 & 自然言語検索

- **自然言語による日本酒検索 (`/explore`)**:
  - 「フルーティで洋食に合うお酒」「キレのある辛口で熱燗向き」などの自由な文章から Gemini で1024次元ベクトルを生成
  - Firestore の Vector Search (`findNearest`, コサイン類似度) による高精度な類似検索
- **類似度に基づくレコメンデーション**:
  - 日本酒詳細画面で、現在の日本酒の埋め込みベクトルに最も近い日本酒を検索・提示

### 2.6 データ可視化・ビジュアライザー

- **マイページ・テイスティング傾向チャート (`/mypage`)**:
  - Recharts によるレーダーチャート表示
  - 自身のレビュー履歴から「フルーティ」「スッキリ」「熟成」「旨味」「酸味」「甘味」の飲用・評価バランスを可視化
- **3D天球・球面MDSマップ (`/explore/sake`, `GlobeVisualizer`)**:
  - Three.js / React Three Fiber を用いた3D天球ビジュアライザー
  - 1024次元の埋め込みベクトルを古典的多次元尺度構成法（Classical MDS）により3次元球面上に投影
  - 類似するお酒が球面上で近くに集まり、直感的に味の近さを探索可能
- **ベクトル検証ツール (`VectorMaker`)**:
  - 自由なテキストから埋め込みベクトルを生成し、JSON形式でのインポート/エクスポートおよびリアルタイム天球配置
- **日本酒ネットワークグラフ (`SakeNetworkVisualizer`, Cytoscape.js)**:
  - `ml-kmeans` によるクラスタリングと類似度リンク（80%以上）に基づくフォースレイアウト・ネットワーク図
  - クラスタごとの色分けと相互関連の可視化

### 2.7 AI味わいアート生成 (Flavor Image)

- **埋め込みベクトルのビジュアルアート化 (`SakeFlavorImage`)**:
  - 日本酒の1024次元ベクトルを Gemini (`gemini-3.1-flash-lite`) に渡し、味・香りの抽象的アート用英語プロンプトに逆翻訳
  - 画像生成モデル (`gemini-3.1-flash-lite-image`) でアスペクト比 1:1 のデジタルアートを自動生成
  - 生成画像をリサイズ・最適化して Firebase Storage に保存・表示

### 2.8 バックエンド & 管理者機能 (Cloud Functions)

- **`onSakeWrite` (Firestore トリガー)**:
  - 日本酒ドキュメントの新規作成・更新時に自動実行
  - スペックテキストから Gemini で1024次元ベクトル（`gemini-embedding-001`）を生成し、`FieldValue.vector` として保存
- **`onReviewCreated` (Firestore トリガー)**:
  - レビュー投稿時に自動実行
  - 前半1024次元（日本酒スペック＋温度＋酒器＋おつまみ）と後半1024次元（香り＋味わい＋評価）を結合し、2048次元の複合ベクトルをレビューに保存
  - レビューの後半ベクトルを使って、日本酒本体の埋め込みベクトルを加重平均ブレンドで動的アップデート（レビューが集まるほど味の表現が洗練される）
- **`searchSakesByVector` / `searchSakesByText` (Callable Function)**:
  - ベクトルまたはテキストを受け取り、Firestore のベクトルインデックスから近傍検索を実行
- **管理者専用バッチ処理 (`rebuildAllSakeEmbeddings`, `rebuildSakeEmbeddingById`)**:
  - 全てまたは特定の日本酒の埋め込みベクトルをスペック＋全レビューの重心から一括再計算
- **ネットワークデータ構築 (`rebuildSakeNetworkData`, `getNetworkDataVector`)**:
  - 全日本酒データをバッチ取得して `ml-kmeans` でクラスタリングし、リンクを生成して `sakeNetwork/latest` にキャッシュ

---

## 3. 画面一覧・ルーティング

| パス               | ページ名                              | 主な機能・役割                                               |
| :----------------- | :------------------------------------ | :----------------------------------------------------------- |
| `/`                | ホーム (`Home`)                       | 最新レビュータイムライン、いいね、ページネーション表示       |
| `/explore`         | 探す (`Explore`)                      | 自然言語ベクトル検索、お酒一覧、管理者向け再構築             |
| `/explore/sake`    | 日本酒近さ検証マップ (`ExploreSake2`) | 3D天球 (Three.js MDS) によるベクトル空間可視化               |
| `/sake/new`        | 日本酒登録 (`NewSake`)                | 新規日本酒の追加フォーム                                     |
| `/sake/:id`        | 日本酒詳細 (`SakeDetail`)             | スペック確認、AI味わいアート、類似酒レコメンド、レビュー一覧 |
| `/sake/:id/review` | レビュー投稿 (`NewReview`)            | 温度・酒器・ペアリング・香気味選択、AIコメント生成           |
| `/quests`          | クエスト (`Quest`)                    | オープンな検証クエスト一覧、挑戦・達成フロー                 |
| `/quests/new`      | クエスト作成 (`NewQuest`)             | 新規ペアリング探求クエストの作成                             |
| `/mypage`          | マイページ (`MyPage`)                 | 自身のレビュー履歴、好み傾向レーダーチャート                 |
| `/settings`        | 設定 (`Settings`)                     | 表示名の変更、Gemini APIキーの登録・管理                     |

---

## 4. 主な技術スタック

- **フロントエンド**: React 19, TypeScript, Vite 6, Tailwind CSS 4, React Router 7
- **3D / グラフ描画**: Three.js, `@react-three/fiber`, `@react-three/drei`, Cytoscape.js, Recharts, ml-matrix
- **バックエンド**: Firebase (Cloud Functions v2, Cloud Firestore, Firebase Storage, Firebase Authentication, Firebase Hosting)
- **AI / 機械学習**:
  - `@google/genai` (Gemini API)
  - `models/gemini-embedding-001` (テキスト埋め込み / 1024次元)
  - `models/gemini-3.6-flash` (コメント生成)
  - `gemini-3.1-flash-lite` & `gemini-3.1-flash-lite-image` (味わいアート生成)
  - `ml-kmeans` (k-means++ クラスタリング)
